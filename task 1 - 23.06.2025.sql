
-- use sql_intern
-- show tables

-- create table library(book_id int auto_increment primary key, book_name varchar(30), 
-- author_id int, category_id int, unique key(author_id))

-- desc library

-- create table members(member_id int auto_increment primary key, member_name varchar(30), book_id int,
-- foreign key(book_id)references library(book_id))

-- desc members
